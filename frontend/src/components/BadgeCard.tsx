import {Award,ShieldCheck,Star} from 'lucide-react'; import type {BadgeLevel} from '../api/types';
const META:Record<BadgeLevel,{label:string;icon:typeof Award}>={bronze:{label:'Бронза',icon:ShieldCheck},silver:{label:'Серебро',icon:ShieldCheck},gold:{label:'Золото',icon:Star}};
export function BadgeCard({title,description,level,iconUrl}:{title:string;description?:string|null;level?:BadgeLevel|null;iconUrl?:string|null}){const m=META[level??'bronze'];const Icon=m.icon;return <article className={'badge badge--'+(level??'bronze')}><div className="badge__icon">{iconUrl?<img src={iconUrl} alt=""/>:<Icon aria-hidden="true"/>}</div><div><span className="badge__level">{m.label}</span><h3>{title||'Достижение'}</h3>{description?<p>{description}</p>:null}</div></article>}